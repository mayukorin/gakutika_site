{
    "name": "gakutika",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "postinstall": "cd front; rm -rf package-lock.json; npm install",
        "build": "cd front; cd node_modules; ls; cd ../;npm run build",
        "start": "cd front; npm run serve",
        "serve": "cd front; npm run serve"
    }
}